# SMTP Configuration
MAILHUB=smtp.gmail.com:587
AUTHUSER=noreply@example.com
AUTHPASS=your-generated-gmail-app-password # For Gmail, use an App Password, not your regular password
FROMLINEOVERRIDE=YES # Can be YES/NO. If YES, the "From" header might be controlled by the mail server.
USETLS=NO # Set to YES if your SMTP server requires implicit TLS (often on port 465)
USESTARTTLS=YES # Set to YES if your SMTP server uses STARTTLS (often on port 587)

# Mail Limit
DAILY_MAIL_LIMIT=2000

# Database Configuration
DATABASE_URL=postgres://user:password@db:5432/mailerdb?sslmode=disable

# TLS Verification Option (CAUTION: Set to YES only if you understand the security risks!)
SKIP_TLS_VERIFY=NO

# Application Port (optional, defaults to 8080)
PORT=8080